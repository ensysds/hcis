<x-app-layout><x-slot:title>Karyawan</x-slot:title><x-page-header title="Employee Database" subtitle="Kelola data induk, status Core, dan siklus kerja seluruh karyawan."><x-slot:actions>@can('report.export')<a href="{{ route('reports.employees.excel') }}" class="btn btn-outline-secondary btn-sm"><i class="bi bi-file-earmark-excel"></i> XLSX Lengkap</a><a href="{{ route('reports.employees.pdf') }}" class="btn btn-outline-secondary btn-sm"><i class="bi bi-file-pdf"></i> PDF</a>@endcan @can('employee.create')<a href="{{ route('employees.create') }}" class="btn btn-primary btn-sm"><i class="bi bi-plus"></i> Karyawan Baru</a>@endcan</x-slot:actions></x-page-header><x-card><form class="row g-2 mb-3"><div class="col-md-5"><input class="form-control form-control-sm" name="q" value="{{ request('q') }}" placeholder="Cari nama, NRP, KTP, e-mail, atau PT"></div><div class="col-md-4"><select class="form-select form-select-sm" name="department_id"><option value="">Semua departemen</option>@foreach($departments as $d)<option value="{{ $d->id }}" @selected(request('department_id')==$d->id)>{{ $d->name }}</option>@endforeach</select></div><div class="col-md-3"><button class="btn btn-outline-primary btn-sm">Terapkan Filter</button></div></form><x-data-table><thead><tr><th>NRP</th><th>Nama</th><th>PT / SBU</th><th>Organizational Unit</th><th>Posisi</th><th>Core</th><th>Status</th><th class="text-end">Aksi</th></tr></thead><tbody>@forelse($employees as $e)<tr><td class="fw-semibold">{{ $e->nrp }}</td><td><a href="{{ route('employees.show',$e) }}">{{ $e->full_name }}</a><div class="text-secondary">{{ $e->email }}</div></td><td>{{ $e->company?->name ?? $e->pt ?? '-' }}<div class="text-secondary">{{ $e->sbu ?? '-' }}</div></td><td>{{ $e->organizational_unit ?? '-' }}</td><td>{{ $e->position?->name ?? '-' }}</td><td>{{ ['employee'=>'Employee','manager'=>'Manager','general_manager'=>'General Manager','director'=>'Director'][$e->core_role] ?? 'Employee' }}<div><x-status-badge :status="$e->canLoginToCore() ? 'active' : 'inactive'"/></div></td><td><x-status-badge :status="$e->status"/></td><td class="text-end"><a class="btn btn-sm btn-link" href="{{ route('employees.show',$e) }}"><i class="bi bi-eye"></i></a>@can('employee.update')<a class="btn btn-sm btn-link" href="{{ route('employees.edit',$e) }}"><i class="bi bi-pencil"></i></a>@endcan @if(auth()->user()->hasRole('super_admin'))<form class="d-inline" method="post" action="{{ route('employees.destroy',$e) }}" onsubmit="return confirm('Hapus karyawan ini? Akses Core akan ikut dicabut.')">@csrf @method('DELETE')<button class="btn btn-sm btn-link text-danger"><i class="bi bi-trash"></i></button></form>@endif</td></tr>@empty<tr><td colspan="8" class="empty-state">Data karyawan tidak ditemukan.</td></tr>@endforelse</tbody></x-data-table><div class="mt-3">{{ $employees->links() }}</div></x-card></x-app-layout>
