@props(['name','label','type'=>'text','value'=>null,'required'=>false])<div {{ $attributes->only('class') }}><label class="form-label" for="{{ $name }}">{{ $label }} @if($required)<span class="text-danger">*</span>@endif</label><input type="{{ $type }}" name="{{ $name }}" id="{{ $name }}" value="{{ old($name,$value) }}" {{ $required?'required':'' }} {{ $attributes->except('class')->merge(['class'=>'form-control form-control-sm']) }}>@error($name)<div class="text-danger small">{{ $message }}</div>@enderror</div>
