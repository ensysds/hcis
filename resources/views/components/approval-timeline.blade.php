@props(['steps'])<div class="approval-timeline">@foreach($steps as $step)<div class="approval-step {{ $step->status }}"><span></span><div><strong>Tahap {{ $step->step_order }} · {{ ucwords(str_replace('_',' ',$step->approver_role)) }}</strong><small>{{ ucfirst($step->status) }} @if($step->acted_at) · {{ $step->acted_at->format('d M Y H:i') }}@endif</small></div></div>@endforeach</div>
